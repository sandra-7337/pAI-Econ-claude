---
name: theoretical-economics-claude-skill
description: "Run or resume the repository's human-in-the-loop theoretical economics research pipeline, including puzzle refinement, literature verification, canonical-model matching, formal modeling, propositions, proof checks, counterexamples, optional numerical simulation, and manuscript generation. Use when a user supplies an economic intuition or hypothesis, asks to critique a theoretical model, requests a model extension, or wants to resume an Exploration project."
---

# Theoretical Economics Research Pipeline

Resolve the repository root, then read `SKILL.md` at that root completely and
follow it as the canonical workflow. Resolve all `prompts/`, `templates/`,
`model_library/`, and `Exploration/` paths relative to the repository root.

Use the capabilities available in the current host. Where the canonical workflow
mentions a host-specific tool name, use the equivalent current capability while
preserving the stated verification and approval requirements.
