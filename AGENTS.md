# Project Guidance — pAI-Econ-claude

This repository supports both Codex and Claude Code.

Before doing any work, read `CLAUDE.md` completely. It is the canonical shared
project-rules file despite its historical filename. Treat those rules as applying
to Codex as well as Claude Code.

For the theoretical-economics pipeline, use the repository skill
`theoretical-economics-claude-skill`. Keep the root `SKILL.md` as the single
canonical workflow definition; platform-specific entry points must delegate to it.
